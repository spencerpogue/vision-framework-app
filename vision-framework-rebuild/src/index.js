
import React from 'react';
import ReactDOM from 'react-dom/client';
import VisionStepperForm from './VisionStepperForm';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <VisionStepperForm />
  </React.StrictMode>
);
